# YAML Examples

YAML remains supported by the Python reference implementation as a secondary format.

## File

- [`examples/yaml/minimal.yaml`](https://github.com/4k4m1m3-org/ResumeSpec/blob/v1.0.0/examples/yaml/minimal.yaml)

## Status

YAML is convenient for editing, but JSON remains the canonical v1 format and the JSON Schema remains the authoritative contract.
