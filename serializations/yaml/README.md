# YAML Serialization

YAML is a secondary implementation format in ResumeSpec v1.0.0.

The Python reference parser can load YAML documents that use the same data model as canonical JSON.

YAML does not have an independent normative v1 specification. Conformance is determined by converting the YAML mapping to data and validating that data against the canonical JSON Schema.
