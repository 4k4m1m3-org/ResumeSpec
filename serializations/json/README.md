# JSON Serialization

JSON is the canonical ResumeSpec v1.0.0 format.

The normative JSON contract is defined by:

`schemas/json/resumespec.schema.json`

ResumeSpec JSON documents are UTF-8 encoded JSON objects with required `metadata` and `sections` properties.
